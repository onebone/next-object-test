const config = {
	AnchorColor: '#4dd0e1',
	AnchorStrokeColor: 'black',
	AnchorSize: 20,
	AnchorStrokeWidth: 5,

	DefaultShapeColor: '#8bc34a',
};
Object.freeze(config);

define(() => {
	return config;
});
